<div wire:poll.30s>
    <x-page-header title="Notifications" description="Invitations, document shares, comments, and role changes."><flux:button wire:click="markAllRead" variant="ghost">Mark all as read</flux:button></x-page-header>
    <section class="overflow-hidden rounded-2xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900">
        <div class="divide-y divide-zinc-100 dark:divide-zinc-800">@forelse($notifications as $notification)<div class="flex w-full gap-4 px-6 py-5 text-left transition hover:bg-zinc-50 dark:hover:bg-zinc-800/50 {{ !$notification->read_at ? 'bg-indigo-50/50 dark:bg-indigo-950/20' : '' }}"><button wire:click="markRead({{ $notification->id }})" class="mt-1 grid size-9 shrink-0 place-items-center rounded-xl {{ !$notification->read_at ? 'bg-indigo-100 text-indigo-600 dark:bg-indigo-950 dark:text-indigo-300' : 'bg-zinc-100 text-zinc-500 dark:bg-zinc-800' }}"><flux:icon.bell class="size-4" /></button><span class="min-w-0 flex-1"><span class="block text-sm font-medium">{{ str($notification->type)->replace('.', ' ')->title() }}</span><span class="mt-1 block text-sm text-zinc-500">{{ $notification->data['message'] ?? 'You have a new notification.' }}</span><span class="mt-2 block text-xs text-zinc-400">{{ $notification->created_at->diffForHumans() }}</span></span>@if($notification->type === 'project.invited' && isset($notification->data['project_id']))<flux:button wire:click="acceptInvite({{ $notification->id }})" size="sm" variant="primary">Accept</flux:button>@elseif(!$notification->read_at)<span class="mt-2 size-2 rounded-full bg-indigo-600"></span>@endif</div>@empty<div class="p-6"><x-empty-state icon="bell-slash" title="You’re all caught up" description="New activity will appear here." /></div>@endforelse</div>
    </section><div class="mt-6">{{ $notifications->links() }}</div>
</div>
