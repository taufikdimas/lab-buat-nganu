<div>
    <x-page-header title="Admin overview" description="System-wide health, content, and governance at a glance." />
    <div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach($counts as $label => $value)<a href="{{ $label === 'users' || $label === 'suspended' ? route('admin.users') : route('projects.index') }}" wire:navigate class="rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><p class="text-sm capitalize text-zinc-500">{{ $label }}</p><p class="mt-3 text-3xl font-semibold">{{ $value }}</p></a>@endforeach</div>
    <section class="mt-7 overflow-hidden rounded-2xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><div class="flex items-center justify-between border-b border-zinc-200 px-6 py-5 dark:border-zinc-800"><div><h2 class="font-semibold">Recent audit events</h2><p class="text-sm text-zinc-500">Sensitive actions across the instance</p></div><flux:button variant="ghost" :href="route('admin.audit-logs')" wire:navigate>View all</flux:button></div><div class="divide-y divide-zinc-100 dark:divide-zinc-800">@foreach($logs as $log)<div class="grid gap-2 px-6 py-4 text-sm sm:grid-cols-[1fr_1fr_160px]"><span class="font-medium">{{ $log->action }}</span><span class="text-zinc-500">{{ $log->actor?->name ?? 'System' }} · {{ $log->target_type }} #{{ $log->target_id ?? '—' }}</span><span class="text-zinc-400 sm:text-right">{{ $log->created_at->diffForHumans() }}</span></div>@endforeach</div></section>
</div>
