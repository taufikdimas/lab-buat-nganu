<div>
    <x-page-header title="Users" description="Manage account state and system-level access." />
    <div class="mb-5 flex gap-3"><flux:input wire:model.live.debounce.300ms="search" icon="magnifying-glass" placeholder="Search users…" class="max-w-sm" /><flux:select wire:model.live="status" class="max-w-40"><flux:select.option value="all">All statuses</flux:select.option><flux:select.option value="active">Active</flux:select.option><flux:select.option value="suspended">Suspended</flux:select.option></flux:select></div>
    <div class="overflow-hidden rounded-2xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-zinc-50 text-xs uppercase tracking-wide text-zinc-500 dark:bg-zinc-800/70"><tr><th class="px-6 py-4">User</th><th class="px-6 py-4">Role</th><th class="px-6 py-4">Status</th><th class="px-6 py-4">Joined</th><th class="px-6 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-zinc-100 dark:divide-zinc-800">@foreach($users as $user)<tr><td class="px-6 py-4"><div class="flex items-center gap-3"><x-avatar :user="$user" /><div><div class="font-medium">{{ $user->name }}</div><div class="text-xs text-zinc-500">{{ $user->email }}</div></div></div></td><td class="px-6 py-4"><x-status-badge :value="$user->system_role" /></td><td class="px-6 py-4"><x-status-badge :value="$user->status" /></td><td class="px-6 py-4 text-zinc-500">{{ $user->created_at->format('M j, Y') }}</td><td class="px-6 py-4 text-right"><div class="flex justify-end gap-2"><flux:button wire:click="toggleRole({{ $user->id }})" size="sm" variant="ghost">Change role</flux:button><flux:button wire:click="toggleStatus({{ $user->id }})" size="sm" :variant="$user->status === 'active' ? 'danger' : 'primary'">{{ $user->status === 'active' ? 'Suspend' : 'Activate' }}</flux:button></div></td></tr>@endforeach</tbody></table></div></div><div class="mt-6">{{ $users->links() }}</div>
</div>
